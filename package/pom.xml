<?xml version="1.0" encoding="UTF-8"?>

<!-- OIE Sentinel — channel monitoring & alerting plugin. -->
<!-- Published under the terms of the Mozilla Public License 2.0. -->

<project xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <artifactId>sentinel</artifactId>
        <groupId>org.openintegrationengine</groupId>
        <version>${revision}</version>
    </parent>

    <artifactId>sentinel-package</artifactId>
    <name>OIE Sentinel Package</name>

    <properties>
        <versionNumber>${project.version}</versionNumber>
        <packageName>sentinel</packageName>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.openintegrationengine</groupId>
            <artifactId>sentinel-server</artifactId>
            <version>${revision}</version>
        </dependency>
        <dependency>
            <groupId>org.openintegrationengine</groupId>
            <artifactId>sentinel-shared</artifactId>
            <version>${revision}</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- Build the dashboard UI (../webadmin/web/plugin.jsx -> plugin.js) before it is
                 packed into the extension zip by the assembly. This is the entire UI, embedded in
                 the OIE Web Administrator via the oie-web-client plugin contract — no separate
                 SPA/WAR build. -->
            <plugin>
                <groupId>com.github.eirslett</groupId>
                <artifactId>frontend-maven-plugin</artifactId>
                <version>1.15.1</version>
                <configuration>
                    <workingDirectory>../webadmin</workingDirectory>
                    <installDirectory>${project.build.directory}</installDirectory>
                </configuration>
                <executions>
                    <execution>
                        <id>install-node-and-npm</id>
                        <phase>generate-resources</phase>
                        <goals><goal>install-node-and-npm</goal></goals>
                        <configuration><nodeVersion>v20.18.0</nodeVersion></configuration>
                    </execution>
                    <execution>
                        <id>npm-install</id>
                        <phase>generate-resources</phase>
                        <goals><goal>npm</goal></goals>
                        <configuration><arguments>install</arguments></configuration>
                    </execution>
                    <execution>
                        <id>npm-build-webadmin</id>
                        <phase>generate-resources</phase>
                        <goals><goal>npm</goal></goals>
                        <configuration><arguments>run build</arguments></configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <artifactId>maven-resources-plugin</artifactId>
                <version>3.2.0</version>
                <executions>
                    <execution>
                        <id>copy-resources</id>
                        <phase>compile</phase>
                        <goals>
                            <goal>copy-resources</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${project.build.directory}/to-be-packed</outputDirectory>
                            <resources>
                                <resource>
                                    <directory>resources</directory>
                                    <filtering>true</filtering>
                                </resource>
                            </resources>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <version>3.0.1</version>
                <executions>
                    <execution>
                        <id>copy-dependencies</id>
                        <phase>package</phase>
                        <goals>
                            <goal>copy-dependencies</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${project.build.directory}/to-be-packed</outputDirectory>
                            <includeScope>runtime</includeScope>
                            <excludeTransitive>false</excludeTransitive>
                            <overWriteReleases>false</overWriteReleases>
                            <overWriteSnapshots>false</overWriteSnapshots>
                            <overWriteIfNewer>true</overWriteIfNewer>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jarsigner-plugin</artifactId>
                <version>1.4</version>
                <executions>
                    <execution>
                        <id>sign</id>
                        <phase>package</phase>
                        <goals>
                            <goal>sign</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <skip>${signing.skip}</skip>
                    <archiveDirectory>${project.build.directory}/to-be-packed</archiveDirectory>
                    <includes>
                        <include>sentinel-*.jar</include>
                    </includes>
                    <keystore>${signing.keystore}</keystore>
                    <storetype>${signing.storetype}</storetype>
                    <providerClass>${signing.providerClass}</providerClass>
                    <providerArg>${signing.providerArg}</providerArg>
                    <alias>${signing.alias}</alias>
                    <storepass>${signing.storepass}</storepass>
                    <tsa>${signing.tsa}</tsa>
                    <arguments>
                        <argument>-certchain</argument>
                        <argument>${signing.certchain}</argument>
                    </arguments>
                </configuration>
            </plugin>
            <!-- use assembly plugin to pack all jars into a zip -->
            <plugin>
                <artifactId>maven-assembly-plugin</artifactId>
                <version>3.3.0</version>
                <executions>
                    <execution>
                        <id>package-all</id>
                        <phase>package</phase>
                        <goals>
                            <goal>single</goal>
                        </goals>
                        <configuration>
                            <finalName>${packageName}-${versionNumber}</finalName>
                            <appendAssemblyId>false</appendAssemblyId>
                            <outputDirectory>${project.build.directory}</outputDirectory>
                            <descriptors>
                                <descriptor>assembly.xml</descriptor>
                            </descriptors>
                            <attach>true</attach>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
